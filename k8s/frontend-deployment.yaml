apiVersion: apps/v1
kind: Deployment
metadata:
  name: patient-api-frontend
  namespace: default
  labels:
    app: patient-api
    component: frontend
spec:
  replicas: 2  # Multiple replicas for scalability
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app: patient-api
      component: frontend
  template:
    metadata:
      labels:
        app: patient-api
        component: frontend
    spec:
      containers:
      - name: frontend
        image: patient-api-frontend:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 80
          name: http
          protocol: TCP
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "200m"
        livenessProbe:
          httpGet:
            path: /
            port: 80
          initialDelaySeconds: 30
          periodSeconds: 30
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /
            port: 80
          initialDelaySeconds: 10
          periodSeconds: 10
          timeoutSeconds: 3
          failureThreshold: 3
